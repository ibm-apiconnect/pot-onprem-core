'use strict';

describe('appLogin module', function() {

  beforeEach(module('appLogin'));

  describe('appLogin controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var appLoginCtrl = $controller('appLoginCtrl');
      expect(appLoginCtrl).toBeDefined();
    }));

  });
});