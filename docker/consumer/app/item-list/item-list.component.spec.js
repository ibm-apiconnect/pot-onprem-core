'use strict';

describe('item-list module', function() {

  beforeEach(module('item-list'));

  describe('item-list controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var itemListCtrl = $controller('ItemListCtrl');
      expect(itemListCtrl).toBeDefined();
    }));

  });
});