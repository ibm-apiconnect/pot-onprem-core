'use strict';

describe('appConfig module', function() {

  beforeEach(module('appConfig'));

  describe('appConfig controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var appConfigCtrl = $controller('AppConfigCtrl');
      expect(appConfigCtrl).toBeDefined();
    }));

  });
});