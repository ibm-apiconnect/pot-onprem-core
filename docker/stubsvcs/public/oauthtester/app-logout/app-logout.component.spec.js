'use strict';

describe('appLogout module', function() {

  beforeEach(module('appLogout'));

  describe('appLogout controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var appLogoutCtrl = $controller('appLogoutCtrl');
      expect(appLogoutCtrl).toBeDefined();
    }));

  });
});